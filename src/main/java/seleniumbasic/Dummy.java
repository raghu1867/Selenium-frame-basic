package seleniumbasic;

import java.io.FileInputStream;
import java.io.FileNotFoundException;
import java.io.FileOutputStream;

public class Dummy {

	public static void main(String[] args) throws FileNotFoundException {

		
		
		/*
		 * FileInputStream fs=new FileInputStream("xyz");
		 * 
		 * 
		 * 
		 * Workbook wb=new workbookfactory.create(fs); sheet s= wb.getsheet(name);
		 * s.getrow(rownum).getcell(cellnum).getstringcelvalue();//read the data
		 * s.getrow(rownum).getcell(cellnum).setcellvalue("value of page");//write the
		 * data FileOutputStream fos=new FileOutputStream("path of excel file");
		 * wb.write(fos);//save file
		 */	
	}

}
